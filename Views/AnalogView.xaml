<UserControl
    x:Class="WpfApp1.Views.AnalogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfApp1.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Background="White"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock
            Grid.Row="0"
            Style="{StaticResource TitleLabel}"
            Text="Analog" />
        <!--<ListView
            x:Name="analogView"
            Grid.Row="1"
            ItemsSource="{Binding AnalogSignals}"
            SelectionMode="Single"
            >
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Name" />
                    <GridViewColumn Header="RealValue">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding ValueColor}">
                                    <TextBlock Text="{Binding RealValue}" />
                                </Border>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn DisplayMemberBinding="{Binding MaxValue}" Header="Max" />
                    <GridViewColumn DisplayMemberBinding="{Binding MinValue}" Header="Min" />
                    <GridViewColumn DisplayMemberBinding="{Binding MaxThreshold}" Header="MaxThreshold" />
                    --><!--<GridViewColumn Header="Checked">
                    <GridViewColumn.CellTemplate>
                    <DataTemplate>
                    <CheckBox IsChecked="{Binding XPath=Saled}" Style="{StaticResource CustomCheckBoxStyle}" />
                    </DataTemplate>
                    </GridViewColumn.CellTemplate>
                    </GridViewColumn>--><!--
                </GridView>
            </ListView.View>
        </ListView>-->
        <DataGrid
            Grid.Row="1"
            Margin="5"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            ColumnHeaderHeight="30"
            IsReadOnly="True"
            ItemsSource="{Binding AnalogSignals}"
            RowHeaderWidth="0"
            SelectionMode="Single"
            SelectionUnit="Cell" 
            VirtualizingPanel.IsContainerVirtualizable="True"
            VirtualizingPanel.IsVirtualizing="True"
            VirtualizingPanel.VirtualizationMode="Recycling"
            ScrollViewer.CanContentScroll="True"
            EnableColumnVirtualization="True"
            EnableRowVirtualization="True">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Name}" Header="Signal__Name" Width="*"/>
                <DataGridTextColumn Binding="{Binding PinNumber}" Header="Pin__Number" />
                <DataGridTextColumn Binding="{Binding ADChannel}" Header="A/D Channel" />
                <DataGridTextColumn Binding="{Binding Value1}" Header="AI Scaled(Volt/Ampere)" />
                <DataGridTextColumn Binding="{Binding OriginValue}" Header="originValue" />
                <DataGridTemplateColumn Header="AI Raw(volt)">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Background="{Binding OutLimits, Converter={StaticResource OutLimitsColorConverter}}" Text="{Binding RealValue}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Width="100" Binding="{Binding MaxThreshold}" Header="Max Threshold Diag(Volt)" />
                <DataGridTextColumn Binding="{Binding MinThreshold}" Header="Min Threshold Diag(Volt)" />
                <DataGridTextColumn Binding="{Binding MaxValue}" Header="AI Max" />
                <DataGridTextColumn Binding="{Binding MinValue}" Header="AI Min" />
                <DataGridTextColumn Binding="{Binding StandardDev}" Header="Standard Dev" />
            </DataGrid.Columns>
        </DataGrid>
        <!--Control-->
        <StackPanel Grid.Row="0" Grid.Column="1" Grid.RowSpan="2">
           
            <GroupBox Header="Modify Threshold">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <ComboBox
                        Style="{StaticResource NormalCombox}"
                        DisplayMemberPath="Name"
                        ItemsSource="{Binding AnalogSignals}"
                        SelectedItem="{Binding CurrentAnalogSignal}" 
                        Width="200"/>
                    <UniformGrid Grid.Row="1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Min:"
                                       Style="{StaticResource labelText}"/>
                            <TextBox MinWidth="100" Text="{Binding MinThreshold}"
                                     Style="{StaticResource numTextBox}"/>
                        </StackPanel>
                        <CheckBox Content="Latch Faluers"
                                  Style="{StaticResource NormalCheckBox}" HorizontalAlignment="Left"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Max:"
                                       Style="{StaticResource labelText}"/>
                            <TextBox MinWidth="100" Text="{Binding MaxThreshold}"
                                     Style="{StaticResource numTextBox}"/>
                        </StackPanel>
                        <CheckBox Content="Update for all" IsChecked="{Binding UpdateAll}" />
                    </UniformGrid>
                    <UniformGrid Grid.Row="2">
                        <Button Command="{Binding UpdateSignalThresholdCommand}"
                                Style="{StaticResource topButton}"
                                Content="Update" />
                        <Button Command="{Binding ResetSignalThresholdCommand}"
                                Style="{StaticResource topButton}"
                                Content="Reset" />
                        <Button Content="Load Limits"
                                Style="{StaticResource topButton}"/>
                        <Button Content="Save Limits"
                            Style="{StaticResource topButton}"/>
                    </UniformGrid>
                </Grid>
            </GroupBox>
            <GroupBox Grid.Column="1" Header="Calucate Standard Dev">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{StaticResource labelText}">Enter # samples (Max:100</TextBlock>
                    <TextBox Style="{StaticResource numTextBox}"/>
                    <Button Content="Calculate STD" Style="{StaticResource NormalButton}"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </Grid>
</UserControl>
